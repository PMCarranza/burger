<li>
    {{name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}}IT HAS BEEN DEVOURED!{{else}}TO BE DEVOURED!{{/if}}
    </button>
</li>