<h1>Want fries with that?</h1>

<div id="headers">

    <h2 id="lft-hdr"> How do these look?</h2>
    <h2 id="rgt-hdr"> These are gone!</h2>

</div>

<div id="main">

    <ul id="left">
        {{#each burger}}
        {{#unless isDevoured}}
        {{> burgers/burgers-block isDevoured=true}}
        {{/unless}}
        {{/each}}
    </ul>

    <ul id="right">
        {{#each burger}}
        {{#if isDevoured}}
        {{> burgers/burgers-block isDevoured=false}}
        {{/if}}
        {{/each}}
    </ul>

</div>

<div id="dibujos">

    <img src="/assets/img/hamburger.png">
    <img src="/assets/img/bebida.png">

</div>

<h4>What would you like to add?</h4>

<form class="create-form">

    <div class="form-group">
        <label for="bur">Burger Name:</label>
        <input type="text" id="bur" name="name">
    </div>

    <button type="submit" id="add-burger">Add your creation!</button>

</form>