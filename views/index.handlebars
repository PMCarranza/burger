<h1>Burgers!</h1>


<h2> To be Devoured! :-)</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/unless}}
    {{/each}}
</ul>

<div id="burger">
    <img src="/assets/img/hamburger.png">
</div>

<h3>Have been Devoured :-(</h3>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block devoured=false}}
    {{/if}}
    {{/each}}
</ul>

<h4> Make your own! =O</h4>
<form class="create-form">

    <div class="form-group">
        <label for="bur">Burger Name:</label>
        <input type="text" id="bur" name="name">
    </div>

    <div class="form-group">
        <label for="devou">Has it been devoured?</label><br>
        <input type="radio" name="devoured" value="1" checked> Devoured!<br>
        <input type="radio" name="devoured" value="0" checked> Not Devoured!<br>
    </div>

    <button type="submit">Add Da Burger!</button>
</form>